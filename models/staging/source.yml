version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('sales_db')}}"
      schema: raw_schema
      tables:
        - name: customer
        - name: raw_customer
        - name: raw_orders
